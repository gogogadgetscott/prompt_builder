# Contributing to Prompt Builder

Thank you for your interest in contributing to Prompt Builder! We welcome contributions from the community to help make this tool better for everyone.

## 🛠️ Development Setup

1.  **Fork the repository** on GitHub.
2.  **Clone your fork** locally:
    ```bash
    git clone https://github.com/YOUR_USERNAME/prompt_builder.git
    cd prompt_builder
    ```
3.  **Install dependencies**:
    ```bash
    npm install
    ```
4.  **Create a branch** for your feature or fix:
    ```bash
    git checkout -b feature/amazing-feature
    ```

## 🐛 Reporting Issues

If you find a bug or have a feature request, please open an issue in the standard GitHub Issues section.
*   **Bugs**: Describe the issue, steps to reproduce, and expected behavior.
*   **Features**: Explain the use case and how the feature would work.

## 📥 Submitting Pull Requests

1.  Ensure your code follows the existing style.
2.  Run tests locally to ensure no regressions:
    ```bash
    npm test
    npm run lint
    ```
3.  Commit your changes with clear messages.
4.  Push to your fork and submit a Pull Request to the `main` branch.

## 🧪 Testing

*   **Unit Tests**: Run `npm test` (using Vitest).
*   **E2E Tests**: Run `npm run test:e2e` (using Playwright).

We appreciate your help!
